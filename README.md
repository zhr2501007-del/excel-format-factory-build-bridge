# Encrypted build bridge

This repository contains only AES-256-GCM encrypted build input and encrypted outputs. Product source, rule data, and runnable artifacts are never published in readable form.
